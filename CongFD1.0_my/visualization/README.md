# visualization —— CongFD1.0_my 项目可视化

- `index.html`：单文件项目仪表盘（开发进度 / 架构分层 / 数据流管线 / 三维加装清单 / 模块清单 / 快速复习卡）。
- **打开方式**：双击本目录 `index.html`（Windows 侧可经 `\\wsl.localhost\archlinux\…\CongFD1.0_my\visualization\` 打开）；无需联网、无外部依赖。
- **维护约定**：由分析侧在每个任务检查点后更新一次；内容与《CAF-26-01034/…/任务理解与推进方案》§3.3 执行记录保持同源；分析侧只改文件、不提交（提交/推送手动）。
- **设计规范**：遵循 dataviz 方法——分类色板前 5 槽（明/暗双模式均经 `validate_palette.js` 校验）；状态一律"图标＋文字"，颜色不单独表意。
- 最近更新：2026-09-17（任务 4 检查点；git 基线 `92587aa`，任务 2–4 改动未提交）。
